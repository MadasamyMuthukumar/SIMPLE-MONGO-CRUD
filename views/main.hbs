<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MONGO CRUD</title>
</head>

<body>
    <h1>MongoDB CRUD Example</h1>
    {{ msg }}
    <br>

    {{#if edit_id}}
    <h3>Edit Book</h3>

    <form action="/update_book/{{ edit_id }}" method="post">
        <label for="title">Title</label>
        <input type="text" name="title" value="{{edit_book.title}}">

        <label for="author">Author</label>
        <input type="text" name="author" value="{{edit_book.author}}">

        <input type="submit" value="update">

    </form>
    <br> <br>
    {{else}}
    <h3>Create new Book</h3>
    <form action="/store_book" method="post">
        <label for="title">Title</label>
        <input type="text" name="title">

        <label for="author">Author</label>
        <input type="text" name="author">

        <input type="submit" value="store">

    </form>
    <br> <br>
    {{/if}}

    <ul>
        {{#each books}}
        <li> {{this.title}} - {{this.author}} <a href="/?edit_id={{this._id}}">Edit</a> <a href="/?delete_id={{this._id}}" onclick="return confirm('Are you sure to delete?')">Delete</a></li>
        {{/each}}
    </ul>
</body>

</html>